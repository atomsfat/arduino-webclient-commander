#### Arduino webclient commander

Para correr el servidor node
node app.js
es necesario instalar express 
sudo npm install -g express  
o
npm install express
e instalar las dependecias
cd node-arduino
npm install -d

tambien es necesario instalar faye
sudo npm install -g faye

Al skecth es necesario configurarle la IP


